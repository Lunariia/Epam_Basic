package Modul_1_Basic;

import static java.lang.Math.*;

public class Task2 {
    public static void main(String[] args) {
        double a = 4, b = 4, c = 3, z;
        z = (b + sqrt(pow(b, 2) + 4 * a * c) / 2 * a) - pow(a, 3) * c +pow(b,-2);
        System.out.print(z);
    }
}
